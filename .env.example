# Copy this file to ".env" (or ".env.local") and fill in the real value.
# The real backend URL is shared privately by the admin — do NOT commit it.
#
# Base URL of the backend WITHOUT a trailing "/api" (the code appends "/api").
# Example: VITE_API_URL=https://your-backend-host.example.com
VITE_API_URL=
